<UserControl x:Class="KSW.ATE01.Start.Views.Dialogs.TestPlans.AddPinDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KSW.ATE01.Start.Views.Dialogs.TestPlans"
             xmlns:language="clr-namespace:KSW.ATE01.Start"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodel="clr-namespace:KSW.ATE01.Start.ViewModels.Dialogs.TestPlans"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:validation="clr-namespace:KSW.ATE01.Start.Validations"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewmodel:AddPinDialogViewModel}"
             d:Background="White"
             d:DesignHeight="450" d:DesignWidth="800"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="WindowStyle" Value="ToolWindow"></Setter>
            <Setter Property="Height" Value="450"></Setter>
            <Setter Property="Width" Value="300"></Setter>
            <Setter Property="ResizeMode" Value="NoResize"></Setter>
            <Setter Property="ShowInTaskbar" Value="False"></Setter>
        </Style>
    </prism:Dialog.WindowStyle>

    <UserControl.Resources>
        <Style TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
            <Setter Property="Padding" Value="4"></Setter>
            <Setter Property="VerticalAlignment" Value="Center"></Setter>
        </Style>
        <Style TargetType="ComboBox" BasedOn="{StaticResource MaterialDesignOutlinedComboBox}">
            <Setter Property="Padding" Value="4"></Setter>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Visibility" Value="Collapsed"></Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--<materialDesign:Snackbar Padding="4" Grid.ColumnSpan="2" VerticalAlignment="Top" MessageQueue="{Binding MessageQueue}" HorizontalAlignment="Center" Background="{Binding MessageBackground}" Foreground="{DynamicResource MaterialDesignLightForeground}">
        </materialDesign:Snackbar>-->

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <TextBlock VerticalAlignment="Center" Text="{language:L PinName}">
            </TextBlock>
            <TextBox Margin="6,0,0,0" Grid.Column="1" Text="{Binding PinInfo.PinName}"></TextBox>

            <TextBlock Margin="0,6,0,0" Grid.Row="1" VerticalAlignment="Center" Text="{language:L PinType}"></TextBlock>
            <ComboBox Margin="6,6,0,0" Grid.Row="1" Grid.Column="1" ItemsSource="{Binding PinTypDic}" DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding PinInfo.PinType}"></ComboBox>
        </Grid>

        <GroupBox Grid.Row="2" Margin="0,6,0,0" Style="{StaticResource MaterialDesignCardGroupBox}" Header="{language:L SiteSetting}">
            <DataGrid 
                ItemsSource="{Binding PinSiteList}" 
                VerticalContentAlignment="Center"
                AutoGenerateColumns="False" 
                CanUserAddRows="False" 
                ScrollViewer.HorizontalScrollBarVisibility="Auto" 
                materialDesign:DataGridAssist.CellPadding="4"
                VirtualizingPanel.VirtualizationMode="Recycling"
                VirtualizingPanel.IsContainerVirtualizable="True"
                CanUserReorderColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Width="auto" Binding="{Binding SiteName}" IsReadOnly="True"></DataGridTextColumn>
                    <DataGridTextColumn Width="*" Binding="{Binding ChannelName,UpdateSourceTrigger=PropertyChanged}"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="auto"></ColumnDefinition>
                <ColumnDefinition Width="auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" Margin="0,0,6,0" Content="{language:L OK}" Command="{Binding OKCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"></Button>

            <Button Grid.Column="2" Content="{language:L Cancel}" Style="{StaticResource MaterialDesignPaperButton}" Command="{Binding CancelCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"></Button>
        </Grid>
    </Grid>
</UserControl>
