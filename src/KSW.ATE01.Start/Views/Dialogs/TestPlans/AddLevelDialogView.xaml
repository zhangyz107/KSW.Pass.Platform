<UserControl x:Class="KSW.ATE01.Start.Views.Dialogs.TestPlans.AddLevelDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KSW.ATE01.Start.Views.Dialogs.TestPlans"
             xmlns:viewmodel="clr-namespace:KSW.ATE01.Start.ViewModels.Dialogs.TestPlans"
             xmlns:language="clr-namespace:KSW.ATE01.Start"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewmodel:AddLevelDialogViewModel}"
             d:Background="White"
             d:DesignHeight="450" d:DesignWidth="800">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadingCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <UserControl.Resources>
        <Style TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignTextBlock}">
            <Setter Property="VerticalAlignment" Value="Center"></Setter>
        </Style>
        <Style TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
            <Setter Property="Padding" Value="4"></Setter>
            <Setter Property="VerticalAlignment" Value="Center"></Setter>
        </Style>
    </UserControl.Resources>

    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="WindowStyle" Value="ToolWindow"></Setter>
            <Setter Property="Height" Value="500"></Setter>
            <Setter Property="Width" Value="350"></Setter>
            <Setter Property="ResizeMode" Value="NoResize"></Setter>
            <Setter Property="ShowInTaskbar" Value="False"></Setter>
        </Style>
    </prism:Dialog.WindowStyle>

    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <!--<materialDesign:Snackbar  Margin="0,0,0,8" Grid.ColumnSpan="2" VerticalAlignment="Top" MessageQueue="{Binding MessageQueue}" HorizontalAlignment="Center" Background="{Binding MessageBackground}" Foreground="{DynamicResource MaterialDesignLightForeground}">
        </materialDesign:Snackbar>-->

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="0,0,6,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{language:L PinOrGroupName}"></TextBlock>
                <ComboBox ItemsSource="{Binding FilteredItems}" Grid.Column="1" Margin="6,0,0,0" SelectedValue="{Binding Level.GroupOrPinId}" DisplayMemberPath="Value" SelectedValuePath="Key" Style="{StaticResource MaterialDesignOutlinedComboBox}" Padding="4" Text="{Binding EditString,UpdateSourceTrigger=PropertyChanged}" IsEditable="True">
                </ComboBox>

                <TextBlock Margin="0,6,0,0" Grid.Row="1" Text="Vil"></TextBlock>
                <TextBox Text="{Binding Level.Vil}" Grid.Row="1" Grid.Column="1" Margin="6,6,0,0"></TextBox>

                <TextBlock Margin="0,6,0,0" Grid.Row="2" Text="Vih"></TextBlock>
                <TextBox Text="{Binding Level.Vih}" Grid.Row="2" Grid.Column="1" Margin="6,6,0,0"></TextBox>

                <TextBlock Margin="0,6,0,0" Grid.Row="3" Text="Vol"></TextBlock>
                <TextBox Text="{Binding Level.Vol}" Grid.Row="3" Grid.Column="1" Margin="6,6,0,0"></TextBox>

                <TextBlock Margin="0,6,0,0" Grid.Row="4" Text="Voh"></TextBlock>
                <TextBox Text="{Binding Level.Voh}" Grid.Row="4" Grid.Column="1" Margin="6,6,0,0"></TextBox>

                <TextBlock Margin="0,6,0,0" Grid.Row="5" Text="Iol"></TextBlock>
                <TextBox Text="{Binding Level.Iol}" Grid.Row="5" Grid.Column="1" Margin="6,6,0,0"></TextBox>

                <TextBlock Margin="0,6,0,0" Grid.Row="6" Text="Ioh"></TextBlock>
                <TextBox Text="{Binding Level.Ioh}" Grid.Row="6" Grid.Column="1" Margin="6,6,0,0"></TextBox>

                <TextBlock Margin="0,6,0,0" Grid.Row="7" Text="Vt"></TextBlock>
                <TextBox Text="{Binding Level.Vt}" Grid.Row="7" Grid.Column="1" Margin="6,6,0,0"></TextBox>

                <TextBlock Margin="0,6,0,0" Grid.Row="8" Text="Vcl"></TextBlock>
                <TextBox Text="{Binding Level.Vcl}" Grid.Row="8" Grid.Column="1" Margin="6,6,0,0"></TextBox>

                <TextBlock Margin="0,6,0,0" Grid.Row="9" Text="Vch"></TextBlock>
                <TextBox Text="{Binding Level.Vch}" Grid.Row="9" Grid.Column="1" Margin="6,6,0,0"></TextBox>

                <TextBlock Margin="0,6,0,0" Grid.Row="10" Text="Ps"></TextBlock>
                <TextBox Text="{Binding Level.Ps}" Grid.Row="10" Grid.Column="1" Margin="6,6,0,0"></TextBox>

                <TextBlock Margin="0,6,0,0" Grid.Row="11" Text="I"></TextBlock>
                <TextBox Text="{Binding Level.I}" Grid.Row="11" Grid.Column="1" Margin="6,6,0,0"></TextBox>

                <TextBlock Margin="0,6,0,0" Grid.Row="12" Text="Tdelay"></TextBlock>
                <TextBox Text="{Binding Level.Tdelay}" Grid.Row="12" Grid.Column="1" Margin="6,6,0,0"></TextBox>

                <TextBlock Margin="0,6,0,0" Grid.Row="13" Text="Sequence"></TextBlock>
                <TextBox Text="{Binding Level.Sequence}" Grid.Row="13" Grid.Column="1" Margin="6,6,0,0"></TextBox>

                <TextBlock Margin="0,6,0,0" Grid.Row="14" Text="{language:L Comment}"></TextBlock>
                <TextBox Text="{Binding Level.Comment}" Grid.Row="14" Grid.Column="1" Margin="6,6,0,0"></TextBox>
            </Grid>
        </ScrollViewer>

        <Grid Margin="0,8,0,0" Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="auto"></ColumnDefinition>
                <ColumnDefinition Width="auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Margin="0,0,6,0" Content="{language:L OK}" Command="{Binding OKCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"></Button>

            <Button Grid.Column="2" Content="{language:L Cancel}" Style="{StaticResource MaterialDesignPaperButton}" Command="{Binding CancelCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"></Button>
        </Grid>
    </Grid>
</UserControl>
