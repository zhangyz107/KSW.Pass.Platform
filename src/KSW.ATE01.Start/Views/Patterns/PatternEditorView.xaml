<UserControl x:Class="KSW.ATE01.Start.Views.Patterns.PatternEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KSW.ATE01.Start.Views.Patterns"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:language="clr-namespace:KSW.ATE01.Start"
             xmlns:viewmodel="clr-namespace:KSW.ATE01.Start.ViewModels.Patterns"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance viewmodel:PatternEditorViewModel}"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <ToggleButton VerticalAlignment="Top" IsChecked="{Binding IsShowPinOverview}">
            <ToggleButton.Style>
                <Style TargetType="ToggleButton" BasedOn="{StaticResource MaterialDesignToolButton}">
                    <Style.Triggers >
                        <Trigger Property="IsChecked" Value="False">
                            <Setter Property="Content">
                                <Setter.Value>
                                    <materialDesign:PackIcon Kind="ArrowRightCircle"></materialDesign:PackIcon>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Content">
                                <Setter.Value>
                                    <materialDesign:PackIcon Kind="ArrowLeftCircle"></materialDesign:PackIcon>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ToggleButton.Style>
        </ToggleButton>

        <Border Grid.Column="1" BorderBrush="Black" BorderThickness="1" Visibility="{Binding IsShowPinOverview,Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <TextBlock Margin="4,0,0,0" VerticalAlignment="Center" Text="{language:L PinOverview}" TextElement.FontWeight="Bold"></TextBlock>
                <Button VerticalContentAlignment="Center" Foreground="LightSkyBlue" Grid.Column="1" Style="{StaticResource MaterialDesignToolButton}" Command="{Binding RefreshCommand}">
                    <Button.Content>
                        <materialDesign:PackIcon  Kind="Refresh" />
                    </Button.Content>
                </Button>

                <ListView Grid.Row="1" Grid.ColumnSpan="2" ItemsSource="{Binding PinCols}" DisplayMemberPath="PinName">
                </ListView>
            </Grid>
        </Border>

        <DataGrid x:Name="dataGrid" Margin="6,0,0,0" Grid.Column="2" AutoGenerateColumns="False" CanUserAddRows="False" CanUserResizeColumns="False" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" CanUserSortColumns="False" CanUserReorderColumns="False" ItemsSource="{Binding VectorInfos}" materialDesign:DataGridAssist.CellPadding="6" 
          SelectedItem="{Binding VectorRow}" 
          RowHeight="35"
          VirtualizingPanel.IsContainerVirtualizable="True" 
          VirtualizingPanel.IsVirtualizing="True" 
          EnableRowVirtualization="True" 
          EnableColumnVirtualization="True" 
          VirtualizingPanel.VirtualizationMode="Recycling" 
          ScrollViewer.CanContentScroll="True" 
          VirtualizingPanel.ScrollUnit="Pixel">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="{language:L InsertVector}" Command="{Binding InsertVectorCommand}" CommandParameter="{Binding PlacementTarget.SelectedItem,RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}, Mode=FindAncestor}}"></MenuItem>
                    <MenuItem Header="{language:L DeleteVector}" Command="{Binding DeleteVectorCommand}" CommandParameter="{Binding PlacementTarget.SelectedItem,RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}, Mode=FindAncestor}}"></MenuItem>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="{language:L Vector}" IsReadOnly="True" Binding="{Binding Label.IndexInVectors,IsAsync=True}" Width="auto"></DataGridTextColumn>
                <DataGridTextColumn Header="{language:L Label}" IsReadOnly="True" Binding="{Binding Label.LabelFullContent,IsAsync=True}" Width="auto"></DataGridTextColumn>
                <DataGridTextColumn Header="{language:L Command}" IsReadOnly="True" Binding="{Binding Command.CommandFullContent,IsAsync=True}" Width="auto"></DataGridTextColumn>
                <!--<DataGridTextColumn Header="{language:L Instrument}" IsReadOnly="True" Binding="{Binding Instrument}" Width="auto"></DataGridTextColumn>-->
                <DataGridTextColumn Header="{language:L TimingName}" IsReadOnly="True" Binding="{Binding TimingSet,IsAsync=True}" Width="auto"></DataGridTextColumn>
                <DataGridTextColumn Header="{language:L Comment}" IsReadOnly="True" Binding="{Binding Comment,IsAsync=True}" Width="auto"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
