<UserControl x:Class="KSW.ATE01.Start.Views.TestPlans.ChannelSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KSW.ATE01.Start.Views.TestPlans"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:model="clr-namespace:KSW.ATE01.Start.ViewModels"
             mc:Ignorable="d" 
             d:Background="White"
             d:DesignHeight="450" d:DesignWidth="800"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <materialDesign:DrawerHost x:Name="DrawerHost">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Margin="6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Text="站点数："></TextBlock>
                <ComboBox Grid.Column="1" Padding="4" Style="{StaticResource MaterialDesignOutlinedComboBox}" ItemsSource="{Binding SiteCountList}" SelectedValue="{Binding SiteCount}"></ComboBox>

                <Button Margin="6,0,0,0" Grid.Column="3" Style="{StaticResource MaterialDesignOutlinedButton}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Import" VerticalAlignment="Center"/>
                            <TextBlock Text="导入数据" />
                        </StackPanel>
                    </Button.Content>
                </Button>

                <Button Margin="0,6,0,0" Grid.Row="1" Padding="12,0" Command="{Binding AddChannelCommand}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus" />
                            <TextBlock Text="添加通道" />
                        </StackPanel>
                    </Button.Content>
                </Button>

                <Button Margin="6,6,0,0" Grid.Row="1" Grid.Column="1" Padding="12,0" Command="{Binding RemoveChannelCommand}" Style="{StaticResource MaterialDesignOutlinedButton}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Minus" />
                            <TextBlock Text="删除通道" />
                        </StackPanel>
                    </Button.Content>
                </Button>

                <Button Margin="6,6,0,0" Grid.Row="1" Grid.Column="3" Padding="12,0" Command="{Binding RemoveChannelCommand}" Style="{StaticResource MaterialDesignOutlinedButton}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FormatListGroupPlus" VerticalAlignment="Center"/>
                            <TextBlock Text="组设置" />
                        </StackPanel>
                    </Button.Content>
                </Button>
            </Grid>
            <DataGrid Grid.Row="1" Margin="6,0"></DataGrid>
        </Grid>

        <materialDesign:DrawerHost.RightDrawerContent>
            <ContentControl prism:RegionManager.RegionName="{x:Static model:RegionNameManagement.GroupInfoContent}"></ContentControl>
        </materialDesign:DrawerHost.RightDrawerContent>
    </materialDesign:DrawerHost>
</UserControl>
