<UserControl x:Class="KSW.ATE01.Pattern.Start.Views.InstrumentManageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KSW.ATE01.Pattern.Start.Views"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:converter="clr-namespace:KSW.ATE01.Pattern.Start.Converter"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <UserControl.Resources>
        <converter:ConnectStateConverter x:Key="StateConver"></converter:ConnectStateConverter>
    </UserControl.Resources>

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadingCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>

        <GroupBox Header="网口设定">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Content="IP地址:" VerticalAlignment="Center"></Label>
                <TextBlock Grid.Column="1"  VerticalAlignment="Center" Text="{Binding Path=IpAddress}" TextAlignment="Left"></TextBlock>
                <Label Content="设备端口号:" Grid.Column="2" VerticalAlignment="Center"></Label>
                <TextBlock Grid.Column="3" Text="{Binding Path=Port}" VerticalAlignment="Center" TextAlignment="Left"></TextBlock>
                <Label Content="本地端口号:" Grid.Column="4" VerticalAlignment="Center"></Label>
                <TextBlock Grid.Column="5" Text="{Binding Path=LocalPort}" VerticalAlignment="Center" TextAlignment="Left"></TextBlock>
            </Grid>
        </GroupBox>

        <GroupBox Header="控制连接" Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <Button Content="连接" Grid.Column="0" Command="{Binding Path=ConnectCommand}" Style="{StaticResource MaterialDesignOutlinedButton}"></Button>
                <Button Margin="8,0,0,0" Content="发送" Grid.Column="1" Command="{Binding Path=SendCommand}"></Button>
                <Label Grid.Column="2" Content="{Binding Path=Address}" Foreground="{Binding Path=ColorState}" VerticalContentAlignment="Center"/>
                <Label Grid.Column="3" Content="{Binding Path=IsConnected}" Foreground="{Binding Path=ColorState}" VerticalContentAlignment="Center"/>
                <Image Grid.Column="4" Height="20" Source="{Binding Path=ConnectState,Converter={StaticResource StateConver},Mode=OneWay}"/>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
