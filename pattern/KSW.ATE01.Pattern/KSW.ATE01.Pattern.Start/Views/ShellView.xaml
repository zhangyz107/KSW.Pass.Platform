<Window x:Class="KSW.ATE01.Pattern.Start.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KSW.ATE01.Pattern.Start.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:enums="clr-namespace:KSW.ATE01.Pattern.Domain.Projects.Core.Enums;assembly=KSW.ATE01.Pattern.Domain.Projects.Core"
        mc:Ignorable="d"
        WindowStyle="None"
        Height="450"
        Width="800"
        ResizeMode="CanResize"
        BorderThickness="1"
        prism:ViewModelLocator.AutoWireViewModel="True"
        >

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadingCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>


    <Window.Resources>
        <Style x:Key="VerticalColHeader" TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <TextBlock.LayoutTransform>
                                <RotateTransform Angle="90"></RotateTransform>
                            </TextBlock.LayoutTransform>
                        </TextBlock>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DiscolorationEditCombobox" TargetType="ComboBox" BasedOn="{StaticResource MaterialDesignDataGridComboBox}">
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="0"></Setter>
            <Setter Property="Margin" Value="0"></Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Path=SelectedValue,RelativeSource={RelativeSource Mode=Self}}" Value="{x:Static enums:VectorValueType.Zero}">
                    <Setter Property="Background" Value="#E7F3F8" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=SelectedValue,RelativeSource={RelativeSource Mode=Self}}" Value="{x:Static enums:VectorValueType.One}">
                    <Setter Property="Background" Value="#FFE9ED" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=SelectedValue,RelativeSource={RelativeSource Mode=Self}}" Value="{x:Static enums:VectorValueType.L}">
                    <Setter Property="Background" Value="#D1E1FF" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=SelectedValue,RelativeSource={RelativeSource Mode=Self}}" Value="{x:Static enums:VectorValueType.H}">
                    <Setter Property="Background" Value="#F5B9C5" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=SelectedValue,RelativeSource={RelativeSource Mode=Self}}" Value="{x:Static enums:VectorValueType.M}">
                    <Setter Property="Background" Value="#FDF9F2" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=SelectedValue,RelativeSource={RelativeSource Mode=Self}}" Value="{x:Static enums:VectorValueType.X}">
                    <Setter Property="Background" Value="#F5F5F5" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=SelectedValue,RelativeSource={RelativeSource Mode=Self}}" Value="{x:Static enums:VectorValueType.V}">
                    <Setter Property="Background" Value="White" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=SelectedValue,RelativeSource={RelativeSource Mode=Self}}" Value="{x:Static enums:VectorValueType.None}">
                    <Setter Property="Background" Value="White" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DiscolorationCombobox" TargetType="ComboBox" BasedOn="{StaticResource DiscolorationEditCombobox}">
            <Setter Property="BorderBrush" Value="Transparent"></Setter>
        </Style>
    </Window.Resources>

    <Window.Style>
        <!--样式定义-->
        <Style TargetType="{x:Type Window}">
            <Setter Property="WindowChrome.WindowChrome">
                <Setter.Value>
                    <WindowChrome UseAeroCaptionButtons="False" GlassFrameThickness="0" CaptionHeight="30"  />
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Window}">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}"  >
                            <AdornerDecorator >
                                <ContentPresenter x:Name="win_content" />
                            </AdornerDecorator>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="WindowState" Value="Maximized">
                                <Setter Property="Margin" TargetName="win_content" Value="8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Style>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>

        <Border Background="#3B3B3B">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="1" Text="{Binding Path=Title,RelativeSource={RelativeSource AncestorType=Window} }"></TextBlock>

                <StackPanel Margin="6,0" Grid.Column="4" Orientation="Horizontal">
                    <Button Style="{StaticResource MaterialDesignToolButton}" WindowChrome.IsHitTestVisibleInChrome="True"  Click="MinimizeBtn_Click">
                        <Button.Content>
                            <materialDesign:PackIcon Kind="WindowMinimize" />
                        </Button.Content>
                    </Button>

                    <Button Style="{StaticResource MaterialDesignToolButton}" WindowChrome.IsHitTestVisibleInChrome="True"  Click="MaximizeBtn_Click">
                        <Button.Content>
                            <materialDesign:PackIcon Kind="WindowMaximize" />
                        </Button.Content>
                    </Button>

                    <Button Style="{StaticResource MaterialDesignToolButton}" WindowChrome.IsHitTestVisibleInChrome="True"  Click="CloseBtn_Click" >
                        <Button.Content>
                            <materialDesign:PackIcon Kind="WindowClose" />
                        </Button.Content>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="#3B3B3B">
            <Menu Foreground="White" materialDesign:MenuAssist.TopLevelMenuItemHeight="25">
                <MenuItem Header="File" Background="#3B3B3B">
                    <MenuItem Header="Open"></MenuItem>
                    <MenuItem Header="Save"></MenuItem>
                    <MenuItem Header="SaveAs..."></MenuItem>
                    <MenuItem Header="Export LVM..."></MenuItem>
                    <MenuItem Header="Export atp..."></MenuItem>
                </MenuItem>

                <MenuItem Header="Edit" Background="#3B3B3B">
                    <MenuItem Header="Insert Vector"></MenuItem>
                    <MenuItem Header="Delete Vector"></MenuItem>
                </MenuItem>
            </Menu>
        </Border>

        <Grid Grid.Row="2" Margin="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"></ColumnDefinition>
                <ColumnDefinition Width="auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Button VerticalAlignment="Top" Style="{StaticResource MaterialDesignToolButton}">
                <Button.Content>
                    <materialDesign:PackIcon Kind="ArrowLeftCircle" />
                </Button.Content>
            </Button>

            <Border Grid.Column="1" BorderBrush="Black" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"></ColumnDefinition>
                        <ColumnDefinition Width="auto"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center" Text="Pin Explorer" TextElement.FontWeight="Bold"></TextBlock>
                    <Button VerticalContentAlignment="Center" Foreground="LightSkyBlue" Grid.Column="1" Style="{StaticResource MaterialDesignToolButton}">
                        <Button.Content>
                            <materialDesign:PackIcon  Kind="Refresh" />
                        </Button.Content>
                    </Button>

                    <ListView Grid.Row="1" Grid.ColumnSpan="2">
                        <ListViewItem Content="A1"></ListViewItem>
                        <ListViewItem Content="A2"></ListViewItem>
                    </ListView>
                </Grid>
            </Border>

            <DataGrid x:Name="dataGrid" Margin="6,0,0,0" Grid.Column="2" AutoGenerateColumns="False" CanUserAddRows="False" CanUserResizeColumns="False" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ItemsSource="{Binding PatternInfos}" materialDesign:DataGridAssist.CellPadding="6">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Verctor" IsReadOnly="True" Binding="{Binding Vector}" Width="auto"></DataGridTextColumn>
                    <DataGridTextColumn Header="Label" IsReadOnly="True" Binding="{Binding Label}" Width="auto"></DataGridTextColumn>
                    <DataGridTextColumn Header="Command" IsReadOnly="True" Binding="{Binding Command.TypeDescription}" Width="auto"></DataGridTextColumn>
                    <DataGridTextColumn Header="Instrument" IsReadOnly="True" Binding="{Binding Instrument}" Width="auto"></DataGridTextColumn>
                    <DataGridTextColumn Header="Timing Name" IsReadOnly="True" Binding="{Binding TimingName}" Width="auto"></DataGridTextColumn>
                    <DataGridTextColumn Header="Comment" IsReadOnly="True" Binding="{Binding Comment}" Width="auto"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <Border Grid.Row="4" Background="#3B3B3B">

        </Border>
    </Grid>
</Window>
