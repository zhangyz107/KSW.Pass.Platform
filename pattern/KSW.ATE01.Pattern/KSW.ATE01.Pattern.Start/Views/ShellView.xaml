<Window x:Class="KSW.ATE01.Pattern.Start.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KSW.ATE01.Pattern.Start.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:enums="clr-namespace:KSW.ATE01.Pattern.Domain.Projects.Core.Enums;assembly=KSW.ATE01.Pattern.Domain.Projects.Core"
        xmlns:language="clr-namespace:KSW.ATE01.Pattern.Start.Language"
        mc:Ignorable="d"
        WindowStyle="None"
        Height="450"
        Width="800"
        ResizeMode="CanResize"
        BorderThickness="1"
        prism:ViewModelLocator.AutoWireViewModel="True"
        x:Name="view"
        >

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadingCommand}" CommandParameter="{Binding ElementName=view}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>


    <Window.Resources>
        <Style x:Key="VerticalColHeader" TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <TextBlock.LayoutTransform>
                                <RotateTransform Angle="90"></RotateTransform>
                            </TextBlock.LayoutTransform>
                        </TextBlock>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DiscolorationEditCombobox" TargetType="ComboBox" BasedOn="{StaticResource MaterialDesignDataGridComboBox}">
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="0"></Setter>
            <Setter Property="Margin" Value="0"></Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Path=SelectedValue,RelativeSource={RelativeSource Mode=Self}}" Value="{x:Static enums:VectorValueType.Zero}">
                    <Setter Property="Background" Value="#E7F3F8" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=SelectedValue,RelativeSource={RelativeSource Mode=Self}}" Value="{x:Static enums:VectorValueType.One}">
                    <Setter Property="Background" Value="#FFE9ED" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=SelectedValue,RelativeSource={RelativeSource Mode=Self}}" Value="{x:Static enums:VectorValueType.L}">
                    <Setter Property="Background" Value="#D1E1FF" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=SelectedValue,RelativeSource={RelativeSource Mode=Self}}" Value="{x:Static enums:VectorValueType.H}">
                    <Setter Property="Background" Value="#F5B9C5" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=SelectedValue,RelativeSource={RelativeSource Mode=Self}}" Value="{x:Static enums:VectorValueType.M}">
                    <Setter Property="Background" Value="#FDF9F2" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=SelectedValue,RelativeSource={RelativeSource Mode=Self}}" Value="{x:Static enums:VectorValueType.X}">
                    <Setter Property="Background" Value="#F5F5F5" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=SelectedValue,RelativeSource={RelativeSource Mode=Self}}" Value="{x:Static enums:VectorValueType.V}">
                    <Setter Property="Background" Value="White" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=SelectedValue,RelativeSource={RelativeSource Mode=Self}}" Value="{x:Static enums:VectorValueType.None}">
                    <Setter Property="Background" Value="White" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DiscolorationCombobox" TargetType="ComboBox" BasedOn="{StaticResource DiscolorationEditCombobox}">
            <Setter Property="BorderBrush" Value="Transparent"></Setter>
        </Style>
    </Window.Resources>

    <Window.Style>
        <!--样式定义-->
        <Style TargetType="{x:Type Window}">
            <Setter Property="WindowChrome.WindowChrome">
                <Setter.Value>
                    <WindowChrome UseAeroCaptionButtons="False" GlassFrameThickness="0" CaptionHeight="30"  />
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Window}">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" CornerRadius="5" >
                            <AdornerDecorator >
                                <ContentPresenter x:Name="win_content" />
                            </AdornerDecorator>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="WindowState" Value="Maximized">
                                <Setter Property="Margin" TargetName="win_content" Value="8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Style>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Border Background="#3B3B3B">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="1" Text="{Binding Path=Title,RelativeSource={RelativeSource AncestorType=Window} }"></TextBlock>

                <StackPanel Margin="6,0" Grid.Column="4" Orientation="Horizontal">
                    <Button Style="{StaticResource MaterialDesignToolButton}" WindowChrome.IsHitTestVisibleInChrome="True" Command="{Binding OpenMessageCommand}" CommandParameter="{Binding ElementName=drawerHost}">
                        <Button.Content>
                            <materialDesign:PackIcon Kind="MessageBulleted" />
                        </Button.Content>
                    </Button>

                    <Button Style="{StaticResource MaterialDesignToolButton}" WindowChrome.IsHitTestVisibleInChrome="True"  Click="MinimizeBtn_Click">
                        <Button.Content>
                            <materialDesign:PackIcon Kind="WindowMinimize" />
                        </Button.Content>
                    </Button>

                    <Button Style="{StaticResource MaterialDesignToolButton}" WindowChrome.IsHitTestVisibleInChrome="True"  Click="MaximizeBtn_Click">
                        <Button.Content>
                            <materialDesign:PackIcon Kind="WindowMaximize" />
                        </Button.Content>
                    </Button>

                    <Button Style="{StaticResource MaterialDesignToolButton}" WindowChrome.IsHitTestVisibleInChrome="True"  Click="CloseBtn_Click" >
                        <Button.Content>
                            <materialDesign:PackIcon Kind="WindowClose" />
                        </Button.Content>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <materialDesign:DrawerHost x:Name="drawerHost" Grid.Row="1">
            <materialDesign:DrawerHost.RightDrawerContent>
                <Grid Width="200">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>

                    <Border Background="#3B3B3B">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"></RowDefinition>
                                <RowDefinition Height="auto"></RowDefinition>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"></ColumnDefinition>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                                <ColumnDefinition Width="auto"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <Button Foreground="White" Style="{StaticResource MaterialDesignIconButton}" Command="{x:Static materialDesign:DrawerHost.CloseDrawerCommand}" CommandParameter="{x:Static Dock.Right}">
                                <Button.Content>
                                    <materialDesign:PackIcon Kind="ArrowRight" />
                                </Button.Content>
                            </Button>
                            <TextBlock Foreground="White" Grid.Column="1" Text="{language:L Message}" VerticalAlignment="Center" FontSize="14"></TextBlock>

                            <Button Background="#535E74" Style="{StaticResource MaterialDesignRaisedButton}" Margin="4,8" Padding="16,0" Grid.Row="1" Grid.Column="2" Content="{language:L Clear}" materialDesign:ButtonAssist.CornerRadius="15"></Button>
                        </Grid>
                    </Border>

                    <DataGrid Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" CanUserResizeColumns="False" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" CanUserSortColumns="False" CanUserReorderColumns="False" materialDesign:DataGridAssist.CellPadding="6">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{language:L Time}" IsReadOnly="True" Binding="{Binding Time}" Width="auto"></DataGridTextColumn>
                            <DataGridTextColumn Header="{language:L Message}" IsReadOnly="True" Binding="{Binding Message}" Width="*"></DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                </Grid>
            </materialDesign:DrawerHost.RightDrawerContent>

            <TabControl TabStripPlacement="Left" Style="{StaticResource MaterialDesignFilledTabControl}" >
                <TabItem Header="{language:L PatternCompiler}">
                    <ContentControl Content="{Binding PatternCompilerView}"></ContentControl>
                </TabItem>
                <TabItem Header="{language:L PatternEditor}">
                    <ContentControl Content="{Binding PatternEditorView}"></ContentControl>
                </TabItem>
            </TabControl>
        </materialDesign:DrawerHost>
    </Grid>


</Window>
