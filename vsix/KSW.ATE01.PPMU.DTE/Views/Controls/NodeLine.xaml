<UserControl x:Class="KSW.ATE01.PPMU.Start.Views.Controls.NodeLine"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KSW.ATE01.PPMU.Start.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Transparent">
        <Ellipse Width="{Binding Diameter}" Height="{Binding Diameter}" Fill="Black" Stroke="Black" StrokeThickness="2"
                  HorizontalAlignment="Left" VerticalAlignment="Top" Visibility="{Binding StartPointVisibility}">
        </Ellipse>
        <Line x:Name="Line" Stroke="Black" 
               StrokeThickness="1" X1="{Binding X1}" Y1="{Binding Y1}"  X2="{Binding X2}" Y2="{Binding Y2}" HorizontalAlignment="Stretch" VerticalAlignment="Center"></Line>
        <Polyline x:Name="Wave"  Points="{Binding WavePoints}" Fill="Transparent" Stroke="Black" StrokeThickness="1" VerticalAlignment="Center"></Polyline>
        <Ellipse Width="{Binding Diameter}" Height="{Binding Diameter}" Fill="Black" Stroke="Black" StrokeThickness="2" 
                  HorizontalAlignment="Right" VerticalAlignment="Top" Visibility="{Binding EndPointVisibility}">
        </Ellipse>
    </Grid>
</UserControl>
